# TCP-Chat_Server
An ongoing C++ TCP chat server project implementing socket programming, client management, multithreading, username handling, messaging commands, broadcasting, logging, and CMake-based builds.
